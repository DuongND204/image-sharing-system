# 📊 Visual Diagrams - Architecture & Flow

## 1. System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        User Browser                              │
│                    http://localhost:3000                         │
└────────┬─────────────────────────────────────────────────────────┘
         │
         ├─────────────────────────────────────────────────────────┐
         │                                                           │
         ▼                                                           ▼
    ┌─────────────┐                                          ┌──────────────┐
    │   React     │                                          │  MongoDB     │
    │  Frontend   │                                          │  Backend     │
    │  Port 3000  │◄────────────────────────────────────────►│  Port 4000   │
    └────┬────────┘                                          └──────────────┘
         │                /api/comments                          │
         │                /api/likes                              │
         │                /api/auth                               │
         │                /api/users                              │
         │                                                         │
         │              Models                                    │
         │              ├─ User.js                                │
         │              ├─ Comment.js                   ◄─────────┘
         │              └─ Like.js
         │
         │
         ├─────────────────────────────────────────────────────────┐
         │                                                           │
         ▼                                                           ▼
    ┌──────────────────┐                                  ┌──────────────────┐
    │  JSON Server     │                                  │   Database.json  │
    │  Port 5000       │                                  │                  │
    │                  │                                  │  - pictures[]    │
    └──────────────────┘                                  │  - users[]       │
         │                                                  │  - comments[]    │
         │ GET /pictures                                   │  - likes[]       │
         │ GET /users                                      └──────────────────┘
         │ GET /categories
         │
         ▼
    ┌────────────────────┐
    │  Image Data        │
    │  User Data (mock)  │
    │  Categories        │
    └────────────────────┘
```

## 2. Authentication Flow

```
┌──────────────┐
│   Login      │
│   Register   │
└──────┬───────┘
       │
       ▼
┌──────────────────────────┐
│  POST /api/auth/register │
│  POST /api/auth/login    │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│   Verify Credentials     │
│   Hash Password (bcrypt) │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│  Generate JWT Token      │
│  Sign with JWT_SECRET    │
│  Expiry: 1 day           │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│  Return User + Token     │
│  Store in localStorage   │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│  useAuthStore           │
│  ├─ user                │
│  ├─ token               │
│  └─ isAuthenticated     │
└──────────────────────────┘
```

## 3. Protected Route Flow

```
┌───────────────────┐
│  User clicks link │
│  e.g., /image/1   │
└────────┬──────────┘
         │
         ▼
┌──────────────────────────────┐
│  ProtectedRoute Component    │
│                              │
│  Check:                      │
│  1. isAuthenticated?         │
│  2. token exists?            │
│  3. user exists?             │
└────┬───────────────┬─────────┘
     │               │
  ✅ YES            ❌ NO
     │               │
     ▼               ▼
┌──────────────┐  ┌───────────────┐
│ Render Page  │  │ Redirect to   │
│              │  │ /login        │
│ ImageDetail  │  │               │
└──────────────┘  └───────────────┘
```

## 4. Like Feature Flow

```
┌─────────────────────┐
│  Click Like Button  │
└────────┬────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  Check if already liked         │
│  (State: isLiked)               │
└────┬──────────────────┬─────────┘
     │                  │
  LIKED              NOT LIKED
     │                  │
     ▼                  ▼
┌─────────────────┐  ┌──────────────────┐
│  Unlike Action  │  │  Like Action     │
└────┬────────────┘  └────┬─────────────┘
     │                    │
     ▼                    ▼
┌─────────────────────────────────────────┐
│  POST /api/likes                        │
│  DELETE /api/likes/:imageId             │
│                                         │
│  Headers:                               │
│  Authorization: Bearer <token>          │
│  Content-Type: application/json         │
└────┬────────────────────────────────────┘
     │
     ▼
┌─────────────────────────┐
│  Backend protectRoute   │
│  ├─ Check token        │
│  ├─ Find user          │
│  └─ Attach to req.user │
└────┬────────────────────┘
     │
     ▼
┌─────────────────────────────────┐
│  MongoDB Operation              │
│  Like Model:                    │
│  {                              │
│    imageId: "1",                │
│    userId: ObjectId,            │
│    userName: "john_doe",        │
│    createdAt: Date              │
│  }                              │
└────┬────────────────────────────┘
     │
     ▼
┌─────────────────────────┐
│  Update Frontend State  │
│  ├─ isLiked = true/false│
│  ├─ likes.length++      │
│  └─ Re-render button    │
└─────────────────────────┘
```

## 5. Comment Feature Flow

```
┌──────────────────────┐
│  Type in Text Input  │
│  Press Enter or btn  │
└────────┬─────────────┘
         │
         ▼
┌────────────────────────────┐
│  Check:                    │
│  - Text not empty?         │
│  - User logged in?         │
│  - Token valid?            │
└────┬──────────────────────┘
     │
     ▼
┌────────────────────────────┐
│  POST /api/comments        │
│                            │
│  Payload:                  │
│  {                         │
│    imageId: "1",           │
│    text: "Great photo!"    │
│  }                         │
│                            │
│  Headers:                  │
│  Authorization: Bearer ... │
└────┬───────────────────────┘
     │
     ▼
┌────────────────────────────┐
│  Backend protectRoute      │
│  Validate & extract user   │
└────┬───────────────────────┘
     │
     ▼
┌────────────────────────────┐
│  Create Comment in MongoDB │
│                            │
│  {                         │
│    imageId: "1",           │
│    userId: ObjectId,       │
│    text: "Great photo!",   │
│    userName: "john_doe",   │
│    userAvatar: "...",      │
│    createdAt: Date,        │
│    updatedAt: Date         │
│  }                         │
└────┬───────────────────────┘
     │
     ▼
┌────────────────────────────┐
│  Return Comment            │
│  (with _id, timestamps)    │
└────┬───────────────────────┘
     │
     ▼
┌────────────────────────────┐
│  Frontend Update State     │
│  ├─ Add to comments[]      │
│  ├─ Clear input text       │
│  ├─ Re-render comments list│
│  └─ Show success toast     │
└────────────────────────────┘
```

## 6. Delete Comment Flow

```
┌─────────────────────────────┐
│  Click Delete (✕) Button    │
│  On User's Own Comment      │
└────────┬────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│  Check Authorization         │
│  currentUser._id ===         │
│  comment.userId?             │
└────┬───────────┬─────────────┘
     │           │
  ✅ OWNER    ❌ NOT OWNER
     │           │
     ▼           ▼
     │      ┌──────────────┐
     │      │ Don't allow  │
     │      │ Show error   │
     │      └──────────────┘
     │
     ▼
┌──────────────────────────────┐
│  DELETE /api/comments/:id    │
│                              │
│  Headers:                    │
│  Authorization: Bearer ...   │
└────┬────────────────────────┘
     │
     ▼
┌──────────────────────────────┐
│  Backend:                    │
│  1. Verify token             │
│  2. Find comment             │
│  3. Check owner              │
│  4. Delete from MongoDB      │
└────┬────────────────────────┘
     │
     ▼
┌──────────────────────────────┐
│  Return 200 OK               │
│  { message: "Deleted" }      │
└────┬────────────────────────┘
     │
     ▼
┌──────────────────────────────┐
│  Frontend Update             │
│  ├─ Remove from comments[]   │
│  ├─ Update UI                │
│  └─ Show success toast       │
└──────────────────────────────┘
```

## 7. Data Structure Relationships

```
┌─────────────────────────┐
│      Image (JSON)       │
│  ┌─────────────────┐    │
│  │ id: "1"         │    │
│  │ user_id: 2      │◄───┼────────┐
│  │ title: "..."    │    │        │
│  │ likes_count: 45 │    │        │
│  │ comments_count: 8    │        │
│  └─────────────────┘    │        │
└─────────────────────────┘        │
                                   │
         ┌─────────────────────────┘
         │
         ▼
┌─────────────────────┐
│   User (MongoDB)    │
│  ┌───────────────┐  │
│  │ _id: ObjectId │  │◄──────────┐
│  │ username: ".."│  │           │
│  │ email: ".."   │  │           │
│  │ password: ".."│  │           │
│  └───────────────┘  │           │
└─────────────────────┘           │
         ▲                         │
         │ userId ref             │
         │                         │
┌─────────────────────────────┐   │
│  Comment (MongoDB)          │   │
│  ┌───────────────────────┐  │   │
│  │ _id: ObjectId         │  │   │
│  │ imageId: "1"          │  │   │
│  │ userId: ObjectId ─────┼──┼───┘
│  │ text: "Great!"        │  │
│  │ userName: "cached"    │  │
│  │ createdAt: Date       │  │
│  └───────────────────────┘  │
└─────────────────────────────┘


┌─────────────────────────────┐
│  Like (MongoDB)             │
│  ┌───────────────────────┐  │
│  │ _id: ObjectId         │  │
│  │ imageId: "1"          │  │
│  │ userId: ObjectId ─────┼──┼───┐
│  │ userName: "cached"    │  │   │
│  │ createdAt: Date       │  │   │
│  │ Unique: (imageId,     │  │   │
│  │          userId)      │  │   │
│  └───────────────────────┘  │   │
└─────────────────────────────┘   │
         ▲                         │
         │ userId ref             │
         │                         │
         └──────────────────────────
             User (MongoDB)
```

## 8. State Management with Zustand

```
┌──────────────────────────────────┐
│   useAuthStore (Zustand)         │
│                                  │
│  State:                          │
│  ├─ user: {                      │
│  │    _id: ObjectId,            │
│  │    username: "john",         │
│  │    email: "john@..."         │
│  │  }                           │
│  ├─ token: "eyJ..."             │
│  ├─ isAuthenticated: true        │
│  │                              │
│  Actions:                        │
│  ├─ login(formData)              │
│  ├─ register(formData)           │
│  ├─ logout()                     │
│  ├─ initializeAuth()             │
│  └─ ...                          │
│                                  │
│  Storage:                        │
│  └─ localStorage (persist)       │
└──────────────────────────────────┘
         │
         │ Used by
         │
    ┌────┴────┐
    │          │
    ▼          ▼
┌─────────────────────┐  ┌─────────────────┐
│ ProtectedRoute      │  │ ImageDetail     │
│ (check auth)        │  │ (use user info) │
└─────────────────────┘  └─────────────────┘
```

## 9. API Response Flow

```
Client Request
    │
    ├─────────────────────────────┐
    │                             │
    ▼                             ▼
┌──────────────────┐    ┌───────────────────┐
│ JSON Server      │    │ MongoDB Backend   │
│ (images, users)  │    │ (auth, comments)  │
└──────┬───────────┘    └────────┬──────────┘
       │                         │
       ├─ 200 OK                 ├─ 200 OK
       │  (images data)          │  (comments data)
       │                         │
       ├─ 404 Not Found          ├─ 401 Unauthorized
       │                         │  (no token)
       │                         │
       └─ 500 Server Error       ├─ 403 Forbidden
                                 │  (not owner)
                                 │
                                 └─ 500 Server Error
```

## 10. Complete User Journey

```
1. Visit http://localhost:3000
   ├─ Home Page (public)
   ├─ View images (from json-server)
   └─ Search & filter

2. Click Login
   ├─ Fill email & password
   ├─ POST /api/auth/login
   ├─ Receive token + user
   └─ Store in localStorage + zustand

3. Click Image
   ├─ ProtectedRoute checks auth
   ├─ ✅ Pass → ImageDetail
   ├─ Fetch image (json-server)
   ├─ Fetch comments (MongoDB)
   ├─ Fetch likes (MongoDB)
   └─ Display with UI

4. Click Like Button
   ├─ POST /api/likes
   ├─ Save to MongoDB
   ├─ Update UI
   └─ Show ❤️ Unlike

5. Add Comment
   ├─ Type text
   ├─ Click Post
   ├─ POST /api/comments
   ├─ Save to MongoDB
   ├─ Show new comment
   └─ Update count

6. Delete Comment
   ├─ Click ✕ on own comment
   ├─ DELETE /api/comments/:id
   ├─ Verify ownership (backend)
   ├─ Remove from MongoDB
   └─ Update UI

7. Logout
   ├─ Clear token
   ├─ Clear user
   ├─ Set isAuthenticated = false
   └─ Redirect to Home
```

---

**Visual representation of system architecture and data flows**
